# 代码实现任务输入【运行时填写】
- 任务 ID：
- 角色定义绝对路径：
- 目标行为与用户价值：
- 验收条件：
- 项目绝对路径及代码/差异基线：
- 目标模块与允许写入范围：
- 兼容性及明确排除项：
- 允许的依赖、迁移或外部系统操作：
- 验证要求和副作用边界：
- 报告目录（可选）：
- memory_root（可选）：
- 允许委派及成员定义：默认无
- 时间、轮数或费用限制（如有）：

简单任务可用自然语言提供。Agent 应先从项目读取技术栈、仓库规则、现有实现和测试；低风险细节可明确假设后推进，高影响产品选择或高风险权限缺失时才向调用者核实。
