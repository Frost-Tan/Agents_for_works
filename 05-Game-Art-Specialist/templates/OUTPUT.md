# 游戏美术资产交付契约【必需，已填写】

## 报告头
- 状态：completed / partial / blocked / awaiting_approval
- 项目、资产组、版本与输出根目录
- 资产用途、目标平台和最终契约
- 实际生成路径：built-in imagegen 或具体 Meowa 公开能力

## 最终交付
- 最终媒体与预览的绝对路径
- `asset-manifest.md` 的绝对路径
- 每项资产的文件名、用途、尺寸、格式、透明度、数量、视角、帧数、锚点、切片或组件信息；不适用项写明不适用
- 最终提示词或提示词组，以及参考图角色和必须保持项
- 视觉检查与技术检查的实际结果
- 未满足项、已知限制和下一步

Meowa 任务记录可恢复的 job ID，但不得暴露凭据、内部 URL、原始响应、模板、mask、调试文件或未声明的中间产物。只有实际检查过的属性才能写为通过。

## 工程交接
需要 04 接入时，提供最终媒体、命名、尺寸、状态、切片、锚点、布局、动画播放和导入注意事项。只写“建议/请求交接”，除非宿主已实际创建并取得 04 的结果。

## 版本与记忆
源素材和旧版本不得覆盖。新迭代写入下一个 `vNNN`。记忆更新包含资产契约、版本、最终路径、生成方式、Meowa job ID、检查结果、用户决定和下一步，不保存任何密钥。
